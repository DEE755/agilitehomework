// Products are managed externally and loaded directly into the database.
// This seed file is intentionally empty.
const DEFAULT_PRODUCTS: { slug: string; [key: string]: unknown }[] = [];
export default DEFAULT_PRODUCTS;
